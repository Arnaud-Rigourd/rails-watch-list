<%= simple_form_for(list) do |f| %>
  <%= f.input :name %>
  <%= f.submit %>
<% end %>


<%# <form action="/new">
  <label for="new-movie">Name</label>
  <input type="text" name="new-movie" placeholder="Movie's name" autofocus />
</form> %>
